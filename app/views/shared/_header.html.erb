
    <nav class="navbar navbar-expand-sm navbar-dark bg-primary">
        <div class = "container">
          <%= link_to "ADNAT", root_path, class: "navbar-brand"%>
            

            <ul class="navbar-nav d-flex flex-row">
                <% if user_signed_in? %>
                <%# Todo: Change to username %>
                <strong ><%= link_to current_user.email,
                    edit_user_registration_path, class: "nav-link text-white"%> </strong>
                <strong><%= link_to 'Log Out',
                    destroy_user_session_path, class: "nav-link", data: { turbo_method: :delete }%> </strong>
                <% else %>
                <strong><%= link_to 'Sign In',
                    new_user_session_path, class: "nav-link"%> </strong>
                <% end %>
            </ul>
        </div>
    </nav>
